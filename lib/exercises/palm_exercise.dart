import 'package:flutter/material.dart';

class PalmExercise extends StatelessWidget {
  const PalmExercise({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
